操作说明：

（1）在SqList.h文件中，添加折半查找BinarySearch作为顺序表SqList的基本操作函数。

    要求：若查找成功，返回与给定值相匹配的数据元素所在的位序；

          若查找失败，返回-1。



（2）编写test.cpp文件，完成对“建表”和“折半查找”过程的验证。

//输入1,2,3,4,5,6,7,8,9,建立有序顺序表，对该表进行折半查找，找谁？自己输入



（3）在SqList.h中添加 带监视哨的顺序查找 FindWithMonitor（）, 并在test.cpp 文件中完成对它的验证。